import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ing3.isi',
  appName: 'nouvv',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
